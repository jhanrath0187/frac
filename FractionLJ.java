
/**
 * Write a description of class FractionLJ here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class FractionLJ
{
    // instance variables - replace the example below with your own
    private int numerator;
    private int denomiter;

    /**
     * Constructor for objects of class FractionLJ
     */
    public FractionLJ(int num, int dem)
    {
        // initialise instance variables
        numerator = num;
        denomiter = dem;
    }
    public float sampleMethod()
    {
        // put your code here
        System.out.println(numerator);
        System.out.println(denomiter);
        return numerator;
    }
}
